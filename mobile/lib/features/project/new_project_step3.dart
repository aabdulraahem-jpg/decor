import 'package:flutter/material.dart';

class NewProjectStep3 extends StatelessWidget {
  const NewProjectStep3({super.key});

  // TODO(stub): Style chips أفقياً

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('اختر النمط')),
      body: const Center(child: Text('قيد البناء')),
    );
  }
}
